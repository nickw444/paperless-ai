# Paperless-AI secret environment configuration example

# Required: API token from your Paperless user profile
PAPERLESS_API_TOKEN=your-api-token

# Optional: path to the non-secret YAML application config.
# Defaults to config.yaml.
PAPERLESS_AI_CONFIG_FILE=config.yaml
