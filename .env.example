# Paperless-AI environment configuration example

# Paperless-ngx Configuration
# Required: URL of your Paperless-ngx instance (without trailing slash)
PAPERLESS_URL=https://your-paperless-instance.com
# Required: API token from your Paperless user profile
PAPERLESS_API_TOKEN=your-api-token

# Agent selection (claude by default; set to codex or opencode to use other CLIs)
AI_AGENT=claude

# Claude CLI configuration
CLAUDE_COMMAND=claude # Optional: Path to Claude CLI if not in PATH
CLAUDE_MODEL=sonnet
CLAUDE_TIMEOUT=120
CLAUDE_MAX_CONTENT_CHARS=2000

# Codex CLI configuration (used when AI_AGENT=codex)
CODEX_COMMAND=codex # Optional: Path to Codex CLI if not in PATH
CODEX_MODEL=gpt-5
CODEX_TIMEOUT=120
CODEX_MAX_CONTENT_CHARS=5000
CODEX_REASONING_EFFORT=low

# Opencode CLI configuration (used when AI_AGENT=opencode)
OPENCODE_COMMAND=opencode # Optional: Path to Opencode CLI if not in PATH
OPENCODE_MODEL=opencode/grok-code
OPENCODE_TIMEOUT=120
OPENCODE_MAX_CONTENT_CHARS=2000

# Rate limiting
# Optional: Rate limit for document processing (0 = no limit, otherwise documents per minute)
RATE_LIMIT_DOCUMENTS_PER_MINUTE=0
